<nav class="navbar navbar-light bg-light">
  <a class="navbar-brand" href='<%= "#{schedules_path}" %>'>
    <%= image_tag 'logo.svg', width: "30", height: "30", class: "d-inline-block align-top",  alt: "Planning Now" %>
    <%= 'Super Plan' %>
  </a>

  <div class="dropdown show">
    <%= link_to cabinet_path, class: 'btn btn-light',
                               role: 'button',
                               title: t('schedules.index.title.auth') do %>
      <%= render 'icons/box_arrow_right' %>
    <% end %>

    <a class="btn btn-light dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      <%= t('language.title') %>
    </a>

    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
      <%= link_to t('language.en'), root_path(locale: :en), class: "dropdown-item"%>
      <%= link_to t('language.ru'), root_path(locale: :ru), class: "dropdown-item"%>
    </div>
  </div>
</nav>
